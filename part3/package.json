{
  "name": "part3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build &&  cd ~/fullstackopen/part2/Phonebook && npm run build && cp -r build ~/fullstackopen/part3",
    "deploy": "git add --a && git commit -m build && git push heroku main ",
    "deploy:full": "npm run build:ui && npm run deploy",
    "lint": "eslint ."
  },
  "author": "khiem",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongoose": "^6.5.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.23.0",
    "nodemon": "^2.0.19"
  }
}